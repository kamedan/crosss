import 'package:flutter/material.dart';

class BodyMeasurementsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Body Measurements')),
      body: Center(child: Text('Body measurements will be tracked here.')),
    );
  }
}
